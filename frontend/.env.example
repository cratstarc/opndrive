# WordPress Headless CMS Environment Variables

# WordPress GraphQL Endpoint
# This should be your WordPress site's GraphQL endpoint (WPGraphQL plugin required)
# Example: https://your-wordpress-site.com/graphql
WORDPRESS_GRAPHQL_URL=

# WordPress Authentication Token (Optional but recommended)
# For private or draft content access
# Generate this in WordPress using JWT Authentication plugin or Application Passwords
# Example: Bearer eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9...
WORDPRESS_AUTH_TOKEN=

# Revalidation Secret Token
# Used to secure the on-demand revalidation endpoint
# Generate a strong random string (e.g., using: openssl rand -base64 32)
# This will be used in WordPress webhook configuration
REVALIDATE_SECRET=
